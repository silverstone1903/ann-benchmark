engine: chroma

health_check:
  host: localhost
  port: 8000
  timeout_s: 60

index:
  type: hnsw
  m: 16
  ef_construction: 200

search_sweep:
  param_name: search_ef
  values: [10, 20, 40, 80, 160]
