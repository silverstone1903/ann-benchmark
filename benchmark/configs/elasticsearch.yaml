engine: elasticsearch

health_check:
  host: localhost
  port: 9200
  timeout_s: 120

index:
  type: hnsw
  m: 16
  ef_construction: 200

search_sweep:
  param_name: num_candidates
  values: [20, 40, 80, 160, 320]
