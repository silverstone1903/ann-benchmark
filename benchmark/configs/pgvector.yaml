engine: pgvector

health_check:
  host: localhost
  port: 5432
  timeout_s: 60

index:
  type: hnsw
  m: 16
  ef_construction: 200

search_sweep:
  param_name: ef_search
  values: [10, 20, 40, 80, 160]
