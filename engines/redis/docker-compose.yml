services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: vecbench-redis
    ports:
      - "6379:6379"
    cpus: 4
    mem_limit: 8g
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 3s
      retries: 30
